[project]
name = "rock-kingdom-qq-bot"
version = "0.1.0"
description = "Offline core for a QQ Rock Kingdom pet query bot."
requires-python = ">=3.11"
dependencies = [
  "Pillow>=10.0",
]

[project.scripts]
rockbot = "rockbot.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
